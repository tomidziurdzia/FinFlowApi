# Server Port
PORT=8080

# Database Configuration
# Option 1: Use DATABASE_URL (Railway/Heroku style) - takes precedence if set
# DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=require

# Option 2: Use individual variables (fallback if DATABASE_URL not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_aqui
DB_NAME=finflow
DB_SSLMODE=disable

# Server HTTP Configuration (timeouts in seconds)
SERVER_READ_TIMEOUT=10
SERVER_READ_HEADER_TIMEOUT=5
SERVER_WRITE_TIMEOUT=30
SERVER_IDLE_TIMEOUT=60
SERVER_SHUTDOWN_TIMEOUT=10

# Application Configuration
APP_SYSTEM_USER=system

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
